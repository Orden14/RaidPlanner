<form method="post" action="{{ path('guild_event_delete', {'id': guild_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guild_event.id) }}">
    <button class="btn">Delete</button>
</form>
