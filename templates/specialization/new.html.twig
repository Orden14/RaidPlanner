{% extends 'base.html.twig' %}

{% block title %}New Specialization{% endblock %}

{% block body %}
    <h1>Create new Specialization</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">Save</button>
    {{ form_end(form) }}


    <a href="{{ path('specialization_index') }}">back to list</a>
{% endblock %}
