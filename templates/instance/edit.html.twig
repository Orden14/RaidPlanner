{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Instance</h1>

    {{ include('instance/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('instance_index') }}">back to list</a>

    {{ include('instance/_delete_form.html.twig') }}
{% endblock %}
